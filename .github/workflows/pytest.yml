name: Run Tests
on:
  push:
    branches: [ master, monetdbs ]
  pull_request:
env:
  TSTFULL: "true"

jobs:

  pytest:
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.7", "3.14" ]  # oldest and latest supported
        os: [ ubuntu-22.04, windows-latest, macos-14 ]  # oldest available but old windows runners are scarse
        exclude:
          - python-version: "3.7"
            os: macos-14
        include:
          - python-version: "3.8"
            os: macos-14
          - python-version: "3.8"
            os: ubuntu-22.04
          - python-version: "3.9"
            os: ubuntu-22.04
          - python-version: "3.10"
            os: ubuntu-22.04
          - python-version: "3.11"
            os: ubuntu-22.04
          - python-version: "3.12"
            os: ubuntu-22.04
          - python-version: "3.13"
            os: ubuntu-22.04
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install MonetDB
        id: install-monetdb
        uses: MonetDB/install-monetdb@v0.8
        with:
          version: Dec2025-SP2    # Can't use the default "Latest" because Homebrew doesn't have it yetZ

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Check versions
        run: |
          python --version
          echo "MonetDB version ${{ steps.install-monetdb.outputs.versionnumber }}"
        shell: bash

      - name: Configure monetdbd
        if: steps.install-monetdb.outputs.dbfarm
        run: |
          set -x
          sudo -u monetdb monetdbd set control=yes "${{ steps.install-monetdb.outputs.dbfarm }}"
          sudo -u monetdb monetdbd set passphrase=testdb "${{ steps.install-monetdb.outputs.dbfarm }}"
          sudo -u monetdb monetdbd stop "${{ steps.install-monetdb.outputs.dbfarm }}"
          sudo -u monetdb monetdbd start "${{ steps.install-monetdb.outputs.dbfarm }}" || true  # possibly already started by systemd

      - name: Run tests
        env:
          TSTCONTROL: "${{ (steps.install-monetdb.outputs.dbfarm && 'tcp,local') || 'off' }}"
        run: |
          pip install -e '.[test]'
          pytest -v

        shell: bash

      - name: Test wheel
        if: runner.os != 'Windows'
        env:
          TSTCONTROL: "${{ (steps.install-monetdb.outputs.dbfarm && 'tcp,local') || 'off' }}"
        run: make testwheel
        shell: bash

      - name: Build docs
        if: runner.os != 'Windows'
        run: make doc
        shell: bash

